name: gh-test
on:
  pull_request:
    branches: ["dev"]
  workflow_dispatch:
env:
  MAJOR: 0
  MINOR: 0
  PATCH: 0
# permissions:
#   contents: write
jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Test Check
        shell: bash
        run: echo "Use this workflow for testing purposes."
